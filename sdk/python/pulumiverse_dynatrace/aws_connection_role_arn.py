# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['AwsConnectionRoleArnArgs', 'AwsConnectionRoleArn']

@pulumi.input_type
class AwsConnectionRoleArnArgs:
    def __init__(__self__, *,
                 aws_connection_id: pulumi.Input[_builtins.str],
                 role_arn: pulumi.Input[_builtins.str]):
        """
        The set of arguments for constructing a AwsConnectionRoleArn resource.

        :param pulumi.Input[_builtins.str] aws_connection_id: The ID of a `AwsConnection` resource instance for which to define the AWS Role ARN
        :param pulumi.Input[_builtins.str] role_arn: The ARN of the AWS role that should be assumed.
        """
        pulumi.set(__self__, "aws_connection_id", aws_connection_id)
        pulumi.set(__self__, "role_arn", role_arn)

    @_builtins.property
    @pulumi.getter(name="awsConnectionId")
    def aws_connection_id(self) -> pulumi.Input[_builtins.str]:
        """
        The ID of a `AwsConnection` resource instance for which to define the AWS Role ARN
        """
        return pulumi.get(self, "aws_connection_id")

    @aws_connection_id.setter
    def aws_connection_id(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "aws_connection_id", value)

    @_builtins.property
    @pulumi.getter(name="roleArn")
    def role_arn(self) -> pulumi.Input[_builtins.str]:
        """
        The ARN of the AWS role that should be assumed.
        """
        return pulumi.get(self, "role_arn")

    @role_arn.setter
    def role_arn(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "role_arn", value)


@pulumi.input_type
class _AwsConnectionRoleArnState:
    def __init__(__self__, *,
                 aws_connection_id: Optional[pulumi.Input[_builtins.str]] = None,
                 role_arn: Optional[pulumi.Input[_builtins.str]] = None):
        """
        Input properties used for looking up and filtering AwsConnectionRoleArn resources.

        :param pulumi.Input[_builtins.str] aws_connection_id: The ID of a `AwsConnection` resource instance for which to define the AWS Role ARN
        :param pulumi.Input[_builtins.str] role_arn: The ARN of the AWS role that should be assumed.
        """
        if aws_connection_id is not None:
            pulumi.set(__self__, "aws_connection_id", aws_connection_id)
        if role_arn is not None:
            pulumi.set(__self__, "role_arn", role_arn)

    @_builtins.property
    @pulumi.getter(name="awsConnectionId")
    def aws_connection_id(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The ID of a `AwsConnection` resource instance for which to define the AWS Role ARN
        """
        return pulumi.get(self, "aws_connection_id")

    @aws_connection_id.setter
    def aws_connection_id(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "aws_connection_id", value)

    @_builtins.property
    @pulumi.getter(name="roleArn")
    def role_arn(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The ARN of the AWS role that should be assumed.
        """
        return pulumi.get(self, "role_arn")

    @role_arn.setter
    def role_arn(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "role_arn", value)


@pulumi.type_token("dynatrace:index/awsConnectionRoleArn:AwsConnectionRoleArn")
class AwsConnectionRoleArn(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 aws_connection_id: Optional[pulumi.Input[_builtins.str]] = None,
                 role_arn: Optional[pulumi.Input[_builtins.str]] = None,
                 __props__=None):
        """
        > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`)

        > This resource requires the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`)

        ## Requirements

        This resource must be used in combination with the `AwsConnection` resource.
        Ensure you configure both resources together for a valid AWS connection.
        An example of how to set up both resources can be found in the Resource Example Usage section below.

        ## Limitations

        If you are creating the `AwsConnectionRoleArn` and the `aws_iam_role` it is referencing in the same invocation
        of `pulumi up`, be aware that due to eventual consistency in AWS, the creation of the `aws_iam_role` might not be fully propagated
        when the `AwsConnectionRoleArn` resource is being created. To mitigate this, we retry the creation of the `AwsConnectionRoleArn` resource
        with a default timeout of 2 minutes.
        This is also the timeout used by terraform-provider-aws when waiting for IAM changes to propagate.
        If you desire a different timeout, you can adjust it using the `timeouts` block in the `AwsConnectionRoleArn` resource as shown in the example below.

        The following is an example of the error you might encounter due to this limitation:

        > **Warning** If a resource is created using an API token or without setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true` (when both are used), the settings object's owner will remain empty.

        An empty owner implies:
        - The settings object becomes public, allowing other users with settings permissions to read and modify it.
        - Changing the settings object's permissions will have no effect, meaning the `SettingsPermissions` resource can't alter its access.

        When a settings object is created using platform credentials:
        - The owner is set to the owner of the OAuth client or platform token.
        - By default, the settings object is private; only the owner can read and modify it.
        - Access modifiers can be managed using the `SettingsPermissions` resource.

        We recommend using platform credentials to ensure a correct setup.
        In case an API token is needed, we recommend setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true`.

        ## Dynatrace Documentation

        - AWS Connector - https://docs.dynatrace.com/docs/analyze-explore-automate/workflows/actions/aws

        - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:hyperscaler-authentication.connections.aws`)

        ## Export Example Usage

        - `terraform-provider-dynatrace -export AwsConnectionRoleArn` downloads all existing AWS connections.

        The full documentation of the export feature is available [here](https://dt-url.net/h203qmc).

        ## Resource Example Usage

        ```python
        import pulumi
        import json
        import pulumi_aws as aws
        import pulumiverse_dynatrace as dynatrace

        test_aws_connection = dynatrace.AwsConnection("test-aws-connection",
            name="#name#",
            web_identity={
                "consumers": "APP:dynatrace.aws.connector",
            })
        dynatrace_oidc_provider = aws.iam.OpenIdConnectProvider("dynatrace-oidc-provider",
            url="https://token.dynatrace.com",
            client_id_lists=["<TENANT_URL>/app-id/dynatrace.aws.connector"])
        example_role = aws.iam.Role("example_role",
            name="#name#",
            assume_role_policy=pulumi.Output.json_dumps({
                "Version": "2012-10-17",
                "Statement": [{
                    "Effect": "Allow",
                    "Principal": {
                        "Federated": dynatrace_oidc_provider.arn,
                    },
                    "Action": "sts:AssumeRoleWithWebIdentity",
                    "Condition": {
                        "StringEquals": pulumi.Output.all(
                            dynatrace-oidc-providerUrl=dynatrace_oidc_provider.url,
                            id=test_aws_connection.id,
                            dynatrace-oidc-providerUrl1=dynatrace_oidc_provider.url
        ).apply(lambda resolved_outputs: {
                            f"{resolved_outputs['dynatrace-oidc-providerUrl']}:sub": f"dt:connection-id/{resolved_outputs['id']}",
                            f"{resolved_outputs['dynatrace-oidc-providerUrl1']}:aud": "<TENANT_URL>/app-id/dynatrace.aws.connector",
                        })
        ,
                    },
                }],
            }))
        test_aws_connection_arn = dynatrace.AwsConnectionRoleArn("test-aws-connection-arn",
            aws_connection_id=test_aws_connection.id,
            role_arn=example_role.arn)
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] aws_connection_id: The ID of a `AwsConnection` resource instance for which to define the AWS Role ARN
        :param pulumi.Input[_builtins.str] role_arn: The ARN of the AWS role that should be assumed.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: AwsConnectionRoleArnArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`)

        > This resource requires the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`)

        ## Requirements

        This resource must be used in combination with the `AwsConnection` resource.
        Ensure you configure both resources together for a valid AWS connection.
        An example of how to set up both resources can be found in the Resource Example Usage section below.

        ## Limitations

        If you are creating the `AwsConnectionRoleArn` and the `aws_iam_role` it is referencing in the same invocation
        of `pulumi up`, be aware that due to eventual consistency in AWS, the creation of the `aws_iam_role` might not be fully propagated
        when the `AwsConnectionRoleArn` resource is being created. To mitigate this, we retry the creation of the `AwsConnectionRoleArn` resource
        with a default timeout of 2 minutes.
        This is also the timeout used by terraform-provider-aws when waiting for IAM changes to propagate.
        If you desire a different timeout, you can adjust it using the `timeouts` block in the `AwsConnectionRoleArn` resource as shown in the example below.

        The following is an example of the error you might encounter due to this limitation:

        > **Warning** If a resource is created using an API token or without setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true` (when both are used), the settings object's owner will remain empty.

        An empty owner implies:
        - The settings object becomes public, allowing other users with settings permissions to read and modify it.
        - Changing the settings object's permissions will have no effect, meaning the `SettingsPermissions` resource can't alter its access.

        When a settings object is created using platform credentials:
        - The owner is set to the owner of the OAuth client or platform token.
        - By default, the settings object is private; only the owner can read and modify it.
        - Access modifiers can be managed using the `SettingsPermissions` resource.

        We recommend using platform credentials to ensure a correct setup.
        In case an API token is needed, we recommend setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true`.

        ## Dynatrace Documentation

        - AWS Connector - https://docs.dynatrace.com/docs/analyze-explore-automate/workflows/actions/aws

        - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:hyperscaler-authentication.connections.aws`)

        ## Export Example Usage

        - `terraform-provider-dynatrace -export AwsConnectionRoleArn` downloads all existing AWS connections.

        The full documentation of the export feature is available [here](https://dt-url.net/h203qmc).

        ## Resource Example Usage

        ```python
        import pulumi
        import json
        import pulumi_aws as aws
        import pulumiverse_dynatrace as dynatrace

        test_aws_connection = dynatrace.AwsConnection("test-aws-connection",
            name="#name#",
            web_identity={
                "consumers": "APP:dynatrace.aws.connector",
            })
        dynatrace_oidc_provider = aws.iam.OpenIdConnectProvider("dynatrace-oidc-provider",
            url="https://token.dynatrace.com",
            client_id_lists=["<TENANT_URL>/app-id/dynatrace.aws.connector"])
        example_role = aws.iam.Role("example_role",
            name="#name#",
            assume_role_policy=pulumi.Output.json_dumps({
                "Version": "2012-10-17",
                "Statement": [{
                    "Effect": "Allow",
                    "Principal": {
                        "Federated": dynatrace_oidc_provider.arn,
                    },
                    "Action": "sts:AssumeRoleWithWebIdentity",
                    "Condition": {
                        "StringEquals": pulumi.Output.all(
                            dynatrace-oidc-providerUrl=dynatrace_oidc_provider.url,
                            id=test_aws_connection.id,
                            dynatrace-oidc-providerUrl1=dynatrace_oidc_provider.url
        ).apply(lambda resolved_outputs: {
                            f"{resolved_outputs['dynatrace-oidc-providerUrl']}:sub": f"dt:connection-id/{resolved_outputs['id']}",
                            f"{resolved_outputs['dynatrace-oidc-providerUrl1']}:aud": "<TENANT_URL>/app-id/dynatrace.aws.connector",
                        })
        ,
                    },
                }],
            }))
        test_aws_connection_arn = dynatrace.AwsConnectionRoleArn("test-aws-connection-arn",
            aws_connection_id=test_aws_connection.id,
            role_arn=example_role.arn)
        ```


        :param str resource_name: The name of the resource.
        :param AwsConnectionRoleArnArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(AwsConnectionRoleArnArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 aws_connection_id: Optional[pulumi.Input[_builtins.str]] = None,
                 role_arn: Optional[pulumi.Input[_builtins.str]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = AwsConnectionRoleArnArgs.__new__(AwsConnectionRoleArnArgs)

            if aws_connection_id is None and not opts.urn:
                raise TypeError("Missing required property 'aws_connection_id'")
            __props__.__dict__["aws_connection_id"] = aws_connection_id
            if role_arn is None and not opts.urn:
                raise TypeError("Missing required property 'role_arn'")
            __props__.__dict__["role_arn"] = role_arn
        super(AwsConnectionRoleArn, __self__).__init__(
            'dynatrace:index/awsConnectionRoleArn:AwsConnectionRoleArn',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            aws_connection_id: Optional[pulumi.Input[_builtins.str]] = None,
            role_arn: Optional[pulumi.Input[_builtins.str]] = None) -> 'AwsConnectionRoleArn':
        """
        Get an existing AwsConnectionRoleArn resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] aws_connection_id: The ID of a `AwsConnection` resource instance for which to define the AWS Role ARN
        :param pulumi.Input[_builtins.str] role_arn: The ARN of the AWS role that should be assumed.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _AwsConnectionRoleArnState.__new__(_AwsConnectionRoleArnState)

        __props__.__dict__["aws_connection_id"] = aws_connection_id
        __props__.__dict__["role_arn"] = role_arn
        return AwsConnectionRoleArn(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="awsConnectionId")
    def aws_connection_id(self) -> pulumi.Output[_builtins.str]:
        """
        The ID of a `AwsConnection` resource instance for which to define the AWS Role ARN
        """
        return pulumi.get(self, "aws_connection_id")

    @_builtins.property
    @pulumi.getter(name="roleArn")
    def role_arn(self) -> pulumi.Output[_builtins.str]:
        """
        The ARN of the AWS role that should be assumed.
        """
        return pulumi.get(self, "role_arn")

