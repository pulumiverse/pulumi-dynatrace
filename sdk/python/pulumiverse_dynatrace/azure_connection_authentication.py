# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['AzureConnectionAuthenticationArgs', 'AzureConnectionAuthentication']

@pulumi.input_type
class AzureConnectionAuthenticationArgs:
    def __init__(__self__, *,
                 application_id: pulumi.Input[_builtins.str],
                 azure_connection_id: pulumi.Input[_builtins.str],
                 directory_id: pulumi.Input[_builtins.str]):
        """
        The set of arguments for constructing a AzureConnectionAuthentication resource.

        :param pulumi.Input[_builtins.str] application_id: Application (client) ID of your app registered in Microsoft Azure App registrations
        :param pulumi.Input[_builtins.str] azure_connection_id: The ID of a `AzureConnection` resource instance for which to define the Azure Authentication
        :param pulumi.Input[_builtins.str] directory_id: Directory (tenant) ID of Microsoft Entra ID
        """
        pulumi.set(__self__, "application_id", application_id)
        pulumi.set(__self__, "azure_connection_id", azure_connection_id)
        pulumi.set(__self__, "directory_id", directory_id)

    @_builtins.property
    @pulumi.getter(name="applicationId")
    def application_id(self) -> pulumi.Input[_builtins.str]:
        """
        Application (client) ID of your app registered in Microsoft Azure App registrations
        """
        return pulumi.get(self, "application_id")

    @application_id.setter
    def application_id(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "application_id", value)

    @_builtins.property
    @pulumi.getter(name="azureConnectionId")
    def azure_connection_id(self) -> pulumi.Input[_builtins.str]:
        """
        The ID of a `AzureConnection` resource instance for which to define the Azure Authentication
        """
        return pulumi.get(self, "azure_connection_id")

    @azure_connection_id.setter
    def azure_connection_id(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "azure_connection_id", value)

    @_builtins.property
    @pulumi.getter(name="directoryId")
    def directory_id(self) -> pulumi.Input[_builtins.str]:
        """
        Directory (tenant) ID of Microsoft Entra ID
        """
        return pulumi.get(self, "directory_id")

    @directory_id.setter
    def directory_id(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "directory_id", value)


@pulumi.input_type
class _AzureConnectionAuthenticationState:
    def __init__(__self__, *,
                 application_id: Optional[pulumi.Input[_builtins.str]] = None,
                 azure_connection_id: Optional[pulumi.Input[_builtins.str]] = None,
                 directory_id: Optional[pulumi.Input[_builtins.str]] = None):
        """
        Input properties used for looking up and filtering AzureConnectionAuthentication resources.

        :param pulumi.Input[_builtins.str] application_id: Application (client) ID of your app registered in Microsoft Azure App registrations
        :param pulumi.Input[_builtins.str] azure_connection_id: The ID of a `AzureConnection` resource instance for which to define the Azure Authentication
        :param pulumi.Input[_builtins.str] directory_id: Directory (tenant) ID of Microsoft Entra ID
        """
        if application_id is not None:
            pulumi.set(__self__, "application_id", application_id)
        if azure_connection_id is not None:
            pulumi.set(__self__, "azure_connection_id", azure_connection_id)
        if directory_id is not None:
            pulumi.set(__self__, "directory_id", directory_id)

    @_builtins.property
    @pulumi.getter(name="applicationId")
    def application_id(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        Application (client) ID of your app registered in Microsoft Azure App registrations
        """
        return pulumi.get(self, "application_id")

    @application_id.setter
    def application_id(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "application_id", value)

    @_builtins.property
    @pulumi.getter(name="azureConnectionId")
    def azure_connection_id(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The ID of a `AzureConnection` resource instance for which to define the Azure Authentication
        """
        return pulumi.get(self, "azure_connection_id")

    @azure_connection_id.setter
    def azure_connection_id(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "azure_connection_id", value)

    @_builtins.property
    @pulumi.getter(name="directoryId")
    def directory_id(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        Directory (tenant) ID of Microsoft Entra ID
        """
        return pulumi.get(self, "directory_id")

    @directory_id.setter
    def directory_id(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "directory_id", value)


@pulumi.type_token("dynatrace:index/azureConnectionAuthentication:AzureConnectionAuthentication")
class AzureConnectionAuthentication(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 application_id: Optional[pulumi.Input[_builtins.str]] = None,
                 azure_connection_id: Optional[pulumi.Input[_builtins.str]] = None,
                 directory_id: Optional[pulumi.Input[_builtins.str]] = None,
                 __props__=None):
        """
        > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`)

        > This resource requires the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`)

        ## Requirements

        This resource must be used in combination with the `AzureConnection` resource to manage a connection using federated identity credential.
        An example of how to set up both resources can be found in the Resource Example Usage section below.

        ## Limitations

        If you are creating the `AzureConnectionAuthentication` and the `azuread_application_federated_identity_credential` it is referencing in the same invocation of `pulumi up`, be aware that due to eventual consistency in Azure, the creation of the federated identity credential might not be fully propagated when the `AzureConnectionAuthentication` resource is being created. To mitigate this, we retry the creation of the `AzureConnectionAuthentication` resource with a default timeout of 2 minutes.
        If you desire a different timeout, you can adjust it using the `timeouts` block in the `AzureConnectionAuthentication` resource as shown in the example below.

        The following is an example of the error you might encounter due to this limitation:

        > **Warning** If a resource is created using an API token or without setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true` (when both are used), the settings object's owner will remain empty.

        An empty owner implies:
        - The settings object becomes public, allowing other users with settings permissions to read and modify it.
        - Changing the settings object's permissions will have no effect, meaning the `SettingsPermissions` resource can't alter its access.

        When a settings object is created using platform credentials:
        - The owner is set to the owner of the OAuth client or platform token.
        - By default, the settings object is private; only the owner can read and modify it.
        - Access modifiers can be managed using the `SettingsPermissions` resource.

        We recommend using platform credentials to ensure a correct setup.
        In case an API token is needed, we recommend setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true`.

        ## Dynatrace Documentation

        - [Set up Microsoft Azure Connector](https://dt-url.net/gs034de)

        - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:hyperscaler-authentication.connections.azure`)

        ## Export Example Usage

        - `terraform-provider-dynatrace -export AzureConnectionAuthentication` downloads all existing Azure connections.

        The full documentation of the export feature is available [here](https://dt-url.net/h203qmc).

        ## Resource Example Usage

        ```python
        import pulumi
        import pulumi_azuread as azuread
        import pulumiverse_dynatrace as dynatrace

        config = pulumi.Config()
        # The Azure Active Directory tenant ID.
        azure_tenant_id = config.require("azureTenantId")
        # The Dynatrace environment URL
        dynatrace_environment_url = config.require("dynatraceEnvironmentUrl")
        # The Dynatrace token issuer URL
        dynatrace_token_issuer = config.require("dynatraceTokenIssuer")
        # Create an application
        example = azuread.ApplicationRegistration("example", display_name="ExampleApp")
        # Create basic Azure connection
        example_azure_connection = dynatrace.AzureConnection("example",
            name="#name#",
            type="federatedIdentityCredential",
            federated_identity_credential={
                "consumers": ["APP:dynatrace.microsoft.azure.connector"],
            })
        # Create a federated identity credential
        example_application_federated_identity_credential = azuread.ApplicationFederatedIdentityCredential("example",
            application_id=example.id,
            display_name="Example",
            audiences=[f"{dynatrace_environment_url}/app-id/dynatrace.microsoft.azure.connector"],
            issuer=dynatrace_token_issuer,
            subject=example_azure_connection.id.apply(lambda id: f"dt:connection-id/{id}"))
        # Update the Azure connection with authentication details
        example_azure_connection_authentication = dynatrace.AzureConnectionAuthentication("example",
            azure_connection_id=example_azure_connection.id,
            application_id=example.client_id,
            directory_id=azure_tenant_id)
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] application_id: Application (client) ID of your app registered in Microsoft Azure App registrations
        :param pulumi.Input[_builtins.str] azure_connection_id: The ID of a `AzureConnection` resource instance for which to define the Azure Authentication
        :param pulumi.Input[_builtins.str] directory_id: Directory (tenant) ID of Microsoft Entra ID
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: AzureConnectionAuthenticationArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`)

        > This resource requires the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`)

        ## Requirements

        This resource must be used in combination with the `AzureConnection` resource to manage a connection using federated identity credential.
        An example of how to set up both resources can be found in the Resource Example Usage section below.

        ## Limitations

        If you are creating the `AzureConnectionAuthentication` and the `azuread_application_federated_identity_credential` it is referencing in the same invocation of `pulumi up`, be aware that due to eventual consistency in Azure, the creation of the federated identity credential might not be fully propagated when the `AzureConnectionAuthentication` resource is being created. To mitigate this, we retry the creation of the `AzureConnectionAuthentication` resource with a default timeout of 2 minutes.
        If you desire a different timeout, you can adjust it using the `timeouts` block in the `AzureConnectionAuthentication` resource as shown in the example below.

        The following is an example of the error you might encounter due to this limitation:

        > **Warning** If a resource is created using an API token or without setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true` (when both are used), the settings object's owner will remain empty.

        An empty owner implies:
        - The settings object becomes public, allowing other users with settings permissions to read and modify it.
        - Changing the settings object's permissions will have no effect, meaning the `SettingsPermissions` resource can't alter its access.

        When a settings object is created using platform credentials:
        - The owner is set to the owner of the OAuth client or platform token.
        - By default, the settings object is private; only the owner can read and modify it.
        - Access modifiers can be managed using the `SettingsPermissions` resource.

        We recommend using platform credentials to ensure a correct setup.
        In case an API token is needed, we recommend setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true`.

        ## Dynatrace Documentation

        - [Set up Microsoft Azure Connector](https://dt-url.net/gs034de)

        - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:hyperscaler-authentication.connections.azure`)

        ## Export Example Usage

        - `terraform-provider-dynatrace -export AzureConnectionAuthentication` downloads all existing Azure connections.

        The full documentation of the export feature is available [here](https://dt-url.net/h203qmc).

        ## Resource Example Usage

        ```python
        import pulumi
        import pulumi_azuread as azuread
        import pulumiverse_dynatrace as dynatrace

        config = pulumi.Config()
        # The Azure Active Directory tenant ID.
        azure_tenant_id = config.require("azureTenantId")
        # The Dynatrace environment URL
        dynatrace_environment_url = config.require("dynatraceEnvironmentUrl")
        # The Dynatrace token issuer URL
        dynatrace_token_issuer = config.require("dynatraceTokenIssuer")
        # Create an application
        example = azuread.ApplicationRegistration("example", display_name="ExampleApp")
        # Create basic Azure connection
        example_azure_connection = dynatrace.AzureConnection("example",
            name="#name#",
            type="federatedIdentityCredential",
            federated_identity_credential={
                "consumers": ["APP:dynatrace.microsoft.azure.connector"],
            })
        # Create a federated identity credential
        example_application_federated_identity_credential = azuread.ApplicationFederatedIdentityCredential("example",
            application_id=example.id,
            display_name="Example",
            audiences=[f"{dynatrace_environment_url}/app-id/dynatrace.microsoft.azure.connector"],
            issuer=dynatrace_token_issuer,
            subject=example_azure_connection.id.apply(lambda id: f"dt:connection-id/{id}"))
        # Update the Azure connection with authentication details
        example_azure_connection_authentication = dynatrace.AzureConnectionAuthentication("example",
            azure_connection_id=example_azure_connection.id,
            application_id=example.client_id,
            directory_id=azure_tenant_id)
        ```


        :param str resource_name: The name of the resource.
        :param AzureConnectionAuthenticationArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(AzureConnectionAuthenticationArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 application_id: Optional[pulumi.Input[_builtins.str]] = None,
                 azure_connection_id: Optional[pulumi.Input[_builtins.str]] = None,
                 directory_id: Optional[pulumi.Input[_builtins.str]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = AzureConnectionAuthenticationArgs.__new__(AzureConnectionAuthenticationArgs)

            if application_id is None and not opts.urn:
                raise TypeError("Missing required property 'application_id'")
            __props__.__dict__["application_id"] = application_id
            if azure_connection_id is None and not opts.urn:
                raise TypeError("Missing required property 'azure_connection_id'")
            __props__.__dict__["azure_connection_id"] = azure_connection_id
            if directory_id is None and not opts.urn:
                raise TypeError("Missing required property 'directory_id'")
            __props__.__dict__["directory_id"] = directory_id
        super(AzureConnectionAuthentication, __self__).__init__(
            'dynatrace:index/azureConnectionAuthentication:AzureConnectionAuthentication',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            application_id: Optional[pulumi.Input[_builtins.str]] = None,
            azure_connection_id: Optional[pulumi.Input[_builtins.str]] = None,
            directory_id: Optional[pulumi.Input[_builtins.str]] = None) -> 'AzureConnectionAuthentication':
        """
        Get an existing AzureConnectionAuthentication resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] application_id: Application (client) ID of your app registered in Microsoft Azure App registrations
        :param pulumi.Input[_builtins.str] azure_connection_id: The ID of a `AzureConnection` resource instance for which to define the Azure Authentication
        :param pulumi.Input[_builtins.str] directory_id: Directory (tenant) ID of Microsoft Entra ID
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _AzureConnectionAuthenticationState.__new__(_AzureConnectionAuthenticationState)

        __props__.__dict__["application_id"] = application_id
        __props__.__dict__["azure_connection_id"] = azure_connection_id
        __props__.__dict__["directory_id"] = directory_id
        return AzureConnectionAuthentication(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="applicationId")
    def application_id(self) -> pulumi.Output[_builtins.str]:
        """
        Application (client) ID of your app registered in Microsoft Azure App registrations
        """
        return pulumi.get(self, "application_id")

    @_builtins.property
    @pulumi.getter(name="azureConnectionId")
    def azure_connection_id(self) -> pulumi.Output[_builtins.str]:
        """
        The ID of a `AzureConnection` resource instance for which to define the Azure Authentication
        """
        return pulumi.get(self, "azure_connection_id")

    @_builtins.property
    @pulumi.getter(name="directoryId")
    def directory_id(self) -> pulumi.Output[_builtins.str]:
        """
        Directory (tenant) ID of Microsoft Entra ID
        """
        return pulumi.get(self, "directory_id")

