# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs
from ._inputs import *

__all__ = ['GcpConnectionArgs', 'GcpConnection']

@pulumi.input_type
class GcpConnectionArgs:
    def __init__(__self__, *,
                 type: pulumi.Input[_builtins.str],
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 service_account_impersonation: pulumi.Input[Optional['GcpConnectionServiceAccountImpersonationArgs']] = None):
        """
        The set of arguments for constructing a GcpConnection resource.

        :param pulumi.Input[_builtins.str] type: GCP Authentication mechanism to be used by the connection. Possible values: `serviceAccountImpersonation`
        :param pulumi.Input[_builtins.str] name: The name of the connection
        :param pulumi.Input['GcpConnectionServiceAccountImpersonationArgs'] service_account_impersonation: No documentation available
        """
        pulumi.set(__self__, "type", type)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if service_account_impersonation is not None:
            pulumi.set(__self__, "service_account_impersonation", service_account_impersonation)

    @_builtins.property
    @pulumi.getter
    def type(self) -> pulumi.Input[_builtins.str]:
        """
        GCP Authentication mechanism to be used by the connection. Possible values: `serviceAccountImpersonation`
        """
        return pulumi.get(self, "type")

    @type.setter
    def type(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "type", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The name of the connection
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter(name="serviceAccountImpersonation")
    def service_account_impersonation(self) -> pulumi.Input[Optional['GcpConnectionServiceAccountImpersonationArgs']]:
        """
        No documentation available
        """
        return pulumi.get(self, "service_account_impersonation")

    @service_account_impersonation.setter
    def service_account_impersonation(self, value: pulumi.Input[Optional['GcpConnectionServiceAccountImpersonationArgs']]):
        pulumi.set(self, "service_account_impersonation", value)


@pulumi.input_type
class _GcpConnectionState:
    def __init__(__self__, *,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 service_account_impersonation: pulumi.Input[Optional['GcpConnectionServiceAccountImpersonationArgs']] = None,
                 type: pulumi.Input[Optional[_builtins.str]] = None):
        """
        Input properties used for looking up and filtering GcpConnection resources.

        :param pulumi.Input[_builtins.str] name: The name of the connection
        :param pulumi.Input['GcpConnectionServiceAccountImpersonationArgs'] service_account_impersonation: No documentation available
        :param pulumi.Input[_builtins.str] type: GCP Authentication mechanism to be used by the connection. Possible values: `serviceAccountImpersonation`
        """
        if name is not None:
            pulumi.set(__self__, "name", name)
        if service_account_impersonation is not None:
            pulumi.set(__self__, "service_account_impersonation", service_account_impersonation)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The name of the connection
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter(name="serviceAccountImpersonation")
    def service_account_impersonation(self) -> pulumi.Input[Optional['GcpConnectionServiceAccountImpersonationArgs']]:
        """
        No documentation available
        """
        return pulumi.get(self, "service_account_impersonation")

    @service_account_impersonation.setter
    def service_account_impersonation(self, value: pulumi.Input[Optional['GcpConnectionServiceAccountImpersonationArgs']]):
        pulumi.set(self, "service_account_impersonation", value)

    @_builtins.property
    @pulumi.getter
    def type(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        GCP Authentication mechanism to be used by the connection. Possible values: `serviceAccountImpersonation`
        """
        return pulumi.get(self, "type")

    @type.setter
    def type(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "type", value)


@pulumi.type_token("dynatrace:index/gcpConnection:GcpConnection")
class GcpConnection(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 service_account_impersonation: pulumi.Input[Optional[Union['GcpConnectionServiceAccountImpersonationArgs', 'GcpConnectionServiceAccountImpersonationArgsDict']]] = None,
                 type: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        """
        > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`)

        > This resource requires the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`)

        ## Requirements

        This resource uses service account impersonation, which requires the Dynatrace GCP Principal to be granted the `roles/iam.serviceAccountTokenCreator` role on the impersonated service account.
        Use the `GcpPrincipal` resource to provision the Dynatrace GCP Principal and grant it access before creating the connection.
        A full example of how to set this up can be found in the Resource Example Usage section below.

        ## Limitations

        If you are creating the `GcpConnection` and the IAM binding that grants the Dynatrace GCP Principal access to the service account in the same invocation
        of `pulumi up`, be aware that due to eventual consistency in GCP, the IAM policy change might not be fully propagated
        when the `GcpConnection` resource is being created. To mitigate this, we retry the creation of the `GcpConnection` resource
        with a default timeout of 2 minutes.
        If you desire a different timeout, you can adjust it using the `timeouts` block in the `GcpConnection` resource.

        The following is an example of the error you might encounter due to this limitation:

        > **Warning** If a resource is created using an API token or without setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true` (when both are used), the settings object's owner will remain empty.

        An empty owner implies:
        - The settings object becomes public, allowing other users with settings permissions to read and modify it.
        - Changing the settings object's permissions will have no effect, meaning the `SettingsPermissions` resource can't alter its access.

        When a settings object is created using platform credentials:
        - The owner is set to the owner of the OAuth client or platform token.
        - By default, the settings object is private; only the owner can read and modify it.
        - Access modifiers can be managed using the `SettingsPermissions` resource.

        We recommend using platform credentials to ensure a correct setup.
        In case an API token is needed, we recommend setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true`.

        ## Dynatrace Documentation

        - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:hyperscaler-authentication.connections.gcp`)

        ## Export Example Usage

        - `terraform-provider-dynatrace -export GcpConnection` downloads all existing GCP connections.

        The full documentation of the export feature is available [here](https://dt-url.net/h203qmc).

        ## Resource Example Usage

        ```python
        import pulumi
        import pulumi_gcp as gcp
        import pulumiverse_dynatrace as dynatrace

        config = pulumi.Config()
        # The Google Cloud Platform project ID
        gcp_project_id = config.require("gcpProjectId")
        # Create a Service Account in GCP
        impersonable_service_account = gcp.serviceaccount.Account("impersonable_service_account",
            account_id="test-service-account",
            display_name="Test Service Account")
        # Provision the DT GCP Principal. This resource takes no input; it triggers creation of the
        # Dynatrace-managed principal (a singleton) and exposes it via the `principal` attribute.
        principal = dynatrace.GcpPrincipal("principal")
        # Grant DT GCP Principal access to the service account
        wif_binding = gcp.serviceaccount.IAMMember("wif_binding",
            service_account_id=impersonable_service_account.name,
            member=principal.principal.apply(lambda principal: f"serviceAccount:{principal}"),
            role="roles/iam.serviceAccountTokenCreator")
        # Create GCP connection
        my_gcp_connection = dynatrace.GcpConnection("my_gcp_connection",
            name="My Gcp Connection",
            type="serviceAccountImpersonation",
            service_account_impersonation={
                "service_account_id": impersonable_service_account.email,
                "consumers": ["SVC:com.dynatrace.da"],
            },
            opts = pulumi.ResourceOptions(depends_on=[wif_binding]))
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] name: The name of the connection
        :param pulumi.Input[Union['GcpConnectionServiceAccountImpersonationArgs', 'GcpConnectionServiceAccountImpersonationArgsDict']] service_account_impersonation: No documentation available
        :param pulumi.Input[_builtins.str] type: GCP Authentication mechanism to be used by the connection. Possible values: `serviceAccountImpersonation`
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: GcpConnectionArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`)

        > This resource requires the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`)

        ## Requirements

        This resource uses service account impersonation, which requires the Dynatrace GCP Principal to be granted the `roles/iam.serviceAccountTokenCreator` role on the impersonated service account.
        Use the `GcpPrincipal` resource to provision the Dynatrace GCP Principal and grant it access before creating the connection.
        A full example of how to set this up can be found in the Resource Example Usage section below.

        ## Limitations

        If you are creating the `GcpConnection` and the IAM binding that grants the Dynatrace GCP Principal access to the service account in the same invocation
        of `pulumi up`, be aware that due to eventual consistency in GCP, the IAM policy change might not be fully propagated
        when the `GcpConnection` resource is being created. To mitigate this, we retry the creation of the `GcpConnection` resource
        with a default timeout of 2 minutes.
        If you desire a different timeout, you can adjust it using the `timeouts` block in the `GcpConnection` resource.

        The following is an example of the error you might encounter due to this limitation:

        > **Warning** If a resource is created using an API token or without setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true` (when both are used), the settings object's owner will remain empty.

        An empty owner implies:
        - The settings object becomes public, allowing other users with settings permissions to read and modify it.
        - Changing the settings object's permissions will have no effect, meaning the `SettingsPermissions` resource can't alter its access.

        When a settings object is created using platform credentials:
        - The owner is set to the owner of the OAuth client or platform token.
        - By default, the settings object is private; only the owner can read and modify it.
        - Access modifiers can be managed using the `SettingsPermissions` resource.

        We recommend using platform credentials to ensure a correct setup.
        In case an API token is needed, we recommend setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true`.

        ## Dynatrace Documentation

        - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:hyperscaler-authentication.connections.gcp`)

        ## Export Example Usage

        - `terraform-provider-dynatrace -export GcpConnection` downloads all existing GCP connections.

        The full documentation of the export feature is available [here](https://dt-url.net/h203qmc).

        ## Resource Example Usage

        ```python
        import pulumi
        import pulumi_gcp as gcp
        import pulumiverse_dynatrace as dynatrace

        config = pulumi.Config()
        # The Google Cloud Platform project ID
        gcp_project_id = config.require("gcpProjectId")
        # Create a Service Account in GCP
        impersonable_service_account = gcp.serviceaccount.Account("impersonable_service_account",
            account_id="test-service-account",
            display_name="Test Service Account")
        # Provision the DT GCP Principal. This resource takes no input; it triggers creation of the
        # Dynatrace-managed principal (a singleton) and exposes it via the `principal` attribute.
        principal = dynatrace.GcpPrincipal("principal")
        # Grant DT GCP Principal access to the service account
        wif_binding = gcp.serviceaccount.IAMMember("wif_binding",
            service_account_id=impersonable_service_account.name,
            member=principal.principal.apply(lambda principal: f"serviceAccount:{principal}"),
            role="roles/iam.serviceAccountTokenCreator")
        # Create GCP connection
        my_gcp_connection = dynatrace.GcpConnection("my_gcp_connection",
            name="My Gcp Connection",
            type="serviceAccountImpersonation",
            service_account_impersonation={
                "service_account_id": impersonable_service_account.email,
                "consumers": ["SVC:com.dynatrace.da"],
            },
            opts = pulumi.ResourceOptions(depends_on=[wif_binding]))
        ```


        :param str resource_name: The name of the resource.
        :param GcpConnectionArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(GcpConnectionArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 service_account_impersonation: pulumi.Input[Optional[Union['GcpConnectionServiceAccountImpersonationArgs', 'GcpConnectionServiceAccountImpersonationArgsDict']]] = None,
                 type: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = GcpConnectionArgs.__new__(GcpConnectionArgs)

            __props__.__dict__["name"] = name
            __props__.__dict__["service_account_impersonation"] = service_account_impersonation
            if type is None and not opts.urn:
                raise TypeError("Missing required property 'type'")
            __props__.__dict__["type"] = type
        super(GcpConnection, __self__).__init__(
            'dynatrace:index/gcpConnection:GcpConnection',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            name: pulumi.Input[Optional[_builtins.str]] = None,
            service_account_impersonation: pulumi.Input[Optional[Union['GcpConnectionServiceAccountImpersonationArgs', 'GcpConnectionServiceAccountImpersonationArgsDict']]] = None,
            type: pulumi.Input[Optional[_builtins.str]] = None) -> 'GcpConnection':
        """
        Get an existing GcpConnection resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] name: The name of the connection
        :param pulumi.Input[Union['GcpConnectionServiceAccountImpersonationArgs', 'GcpConnectionServiceAccountImpersonationArgsDict']] service_account_impersonation: No documentation available
        :param pulumi.Input[_builtins.str] type: GCP Authentication mechanism to be used by the connection. Possible values: `serviceAccountImpersonation`
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _GcpConnectionState.__new__(_GcpConnectionState)

        __props__.__dict__["name"] = name
        __props__.__dict__["service_account_impersonation"] = service_account_impersonation
        __props__.__dict__["type"] = type
        return GcpConnection(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Output[_builtins.str]:
        """
        The name of the connection
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter(name="serviceAccountImpersonation")
    def service_account_impersonation(self) -> pulumi.Output[Optional['outputs.GcpConnectionServiceAccountImpersonation']]:
        """
        No documentation available
        """
        return pulumi.get(self, "service_account_impersonation")

    @_builtins.property
    @pulumi.getter
    def type(self) -> pulumi.Output[_builtins.str]:
        """
        GCP Authentication mechanism to be used by the connection. Possible values: `serviceAccountImpersonation`
        """
        return pulumi.get(self, "type")

