// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * > **Dynatrace SaaS only**
 *
 * > To utilize this data source, please define the environment variables `DT_CLIENT_ID`, `DT_CLIENT_SECRET`, and `DT_ACCOUNT_ID` with an OAuth client including the following permission: **View environments** (`account-env-read`).
 *
 * This data source allows you to query the environments in a Dynatrace account and filter them by their active status, id, or name.
 *
 * ## Example Usage
 *
 * ### Query active environments
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * export = async () => {
 *     const activeEnvironments = await dynatrace.getIamEnvironments({
 *         active: true,
 *     });
 *     return {
 *         activeEnvironments: activeEnvironments.environments.map(__item => __item.id),
 *     };
 * }
 * ```
 *
 * ### Query environment by name
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * const byName = dynatrace.getIamEnvironments({
 *     name: "production",
 * });
 * export const environmentId = byName.then(byName => byName.environments?.[0]?.id);
 * ```
 */
export function getIamEnvironments(args?: GetIamEnvironmentsArgs, opts?: pulumi.InvokeOptions): Promise<GetIamEnvironmentsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("dynatrace:index/getIamEnvironments:getIamEnvironments", {
        "active": args.active,
        "envId": args.envId,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getIamEnvironments.
 */
export interface GetIamEnvironmentsArgs {
    /**
     * Filter by active status of the environment
     */
    active?: boolean;
    /**
     * Filter by ID of the environment
     */
    envId?: string;
    /**
     * Filter by friendly name of the environment
     */
    name?: string;
}

/**
 * A collection of values returned by getIamEnvironments.
 */
export interface GetIamEnvironmentsResult {
    /**
     * Filter by active status of the environment
     */
    readonly active?: boolean;
    /**
     * Filter by ID of the environment
     */
    readonly envId?: string;
    /**
     * List of environments matching the filter criteria
     */
    readonly environments: outputs.GetIamEnvironmentsEnvironment[];
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * Filter by friendly name of the environment
     */
    readonly name?: string;
}
/**
 * > **Dynatrace SaaS only**
 *
 * > To utilize this data source, please define the environment variables `DT_CLIENT_ID`, `DT_CLIENT_SECRET`, and `DT_ACCOUNT_ID` with an OAuth client including the following permission: **View environments** (`account-env-read`).
 *
 * This data source allows you to query the environments in a Dynatrace account and filter them by their active status, id, or name.
 *
 * ## Example Usage
 *
 * ### Query active environments
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * export = async () => {
 *     const activeEnvironments = await dynatrace.getIamEnvironments({
 *         active: true,
 *     });
 *     return {
 *         activeEnvironments: activeEnvironments.environments.map(__item => __item.id),
 *     };
 * }
 * ```
 *
 * ### Query environment by name
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * const byName = dynatrace.getIamEnvironments({
 *     name: "production",
 * });
 * export const environmentId = byName.then(byName => byName.environments?.[0]?.id);
 * ```
 */
export function getIamEnvironmentsOutput(args?: GetIamEnvironmentsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetIamEnvironmentsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("dynatrace:index/getIamEnvironments:getIamEnvironments", {
        "active": args.active,
        "envId": args.envId,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getIamEnvironments.
 */
export interface GetIamEnvironmentsOutputArgs {
    /**
     * Filter by active status of the environment
     */
    active?: pulumi.Input<boolean>;
    /**
     * Filter by ID of the environment
     */
    envId?: pulumi.Input<string>;
    /**
     * Filter by friendly name of the environment
     */
    name?: pulumi.Input<string>;
}
