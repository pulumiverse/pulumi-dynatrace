// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`)
 *
 * > This resource requires the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`)
 *
 * ## Requirements
 *
 * This resource must be used in combination with the `dynatrace.AwsConnectionRoleArn` resource.
 * Ensure you configure both resources together for a valid AWS connection.
 * An example of how to set up both resources can be found in the Resource Example Usage section below.
 *
 * ## Limitations
 *
 * > **Warning** If a resource is created using an API token or without setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true` (when both are used), the settings object's owner will remain empty.
 *
 * An empty owner implies:
 * - The settings object becomes public, allowing other users with settings permissions to read and modify it.
 * - Changing the settings object's permissions will have no effect, meaning the `dynatrace.SettingsPermissions` resource can't alter its access.
 *
 * When a settings object is created using platform credentials:
 * - The owner is set to the owner of the OAuth client or platform token.
 * - By default, the settings object is private; only the owner can read and modify it.
 * - Access modifiers can be managed using the `dynatrace.SettingsPermissions` resource.
 *
 * We recommend using platform credentials to ensure a correct setup.
 * In case an API token is needed, we recommend setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true`.
 *
 * ## Dynatrace Documentation
 *
 * - AWS Connector - https://docs.dynatrace.com/docs/analyze-explore-automate/workflows/actions/aws
 *
 * - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:hyperscaler-authentication.connections.aws`)
 *
 * ## Export Example Usage
 *
 * - `terraform-provider-dynatrace -export dynatrace.AwsConnection` downloads all existing AWS connections.
 *
 * The full documentation of the export feature is available [here](https://dt-url.net/h203qmc).
 *
 * ## Resource Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * const test_aws_connection = new dynatrace.AwsConnection("test-aws-connection", {
 *     name: "#name#",
 *     webIdentity: {
 *         consumers: "APP:dynatrace.aws.connector",
 *     },
 * });
 * const dynatrace_oidc_provider = new aws.iam.OpenIdConnectProvider("dynatrace-oidc-provider", {
 *     url: "https://token.dynatrace.com",
 *     clientIdLists: ["<TENANT_URL>/app-id/dynatrace.aws.connector"],
 * });
 * const exampleRole = new aws.iam.Role("example_role", {
 *     name: "#name#",
 *     assumeRolePolicy: pulumi.jsonStringify({
 *         Version: "2012-10-17",
 *         Statement: [{
 *             Effect: "Allow",
 *             Principal: {
 *                 Federated: dynatrace_oidc_provider.arn,
 *             },
 *             Action: "sts:AssumeRoleWithWebIdentity",
 *             Condition: {
 *                 StringEquals: pulumi.all([dynatrace_oidc_provider.url, test_aws_connection.id, dynatrace_oidc_provider.url]).apply(([dynatrace-oidc-providerUrl, id, dynatrace-oidc-providerUrl1]) => {
 *                     [`${dynatrace_oidc_providerUrl}:sub`]: `dt:connection-id/${id}`,
 *                     [`${dynatrace_oidc_providerUrl1}:aud`]: "<TENANT_URL>/app-id/dynatrace.aws.connector",
 *                 }),
 *             },
 *         }],
 *     }),
 * });
 * const test_aws_connection_arn = new dynatrace.AwsConnectionRoleArn("test-aws-connection-arn", {
 *     awsConnectionId: test_aws_connection.id,
 *     roleArn: exampleRole.arn,
 * });
 * ```
 */
export class AwsConnection extends pulumi.CustomResource {
    /**
     * Get an existing AwsConnection resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AwsConnectionState, opts?: pulumi.CustomResourceOptions): AwsConnection {
        return new AwsConnection(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'dynatrace:index/awsConnection:AwsConnection';

    /**
     * Returns true if the given object is an instance of AwsConnection.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AwsConnection {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AwsConnection.__pulumiType;
    }

    /**
     * Name
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Configuration required for authenticating via AWS Role Based Authentication
     */
    declare public readonly roleBasedAuth: pulumi.Output<outputs.AwsConnectionRoleBasedAuth | undefined>;
    /**
     * Configuration required for authenticating via AWS Web Identity
     */
    declare public readonly webIdentity: pulumi.Output<outputs.AwsConnectionWebIdentity | undefined>;

    /**
     * Create a AwsConnection resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: AwsConnectionArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AwsConnectionArgs | AwsConnectionState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AwsConnectionState | undefined;
            resourceInputs["name"] = state?.name;
            resourceInputs["roleBasedAuth"] = state?.roleBasedAuth;
            resourceInputs["webIdentity"] = state?.webIdentity;
        } else {
            const args = argsOrState as AwsConnectionArgs | undefined;
            resourceInputs["name"] = args?.name;
            resourceInputs["roleBasedAuth"] = args?.roleBasedAuth;
            resourceInputs["webIdentity"] = args?.webIdentity;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AwsConnection.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AwsConnection resources.
 */
export interface AwsConnectionState {
    /**
     * Name
     */
    name?: pulumi.Input<string>;
    /**
     * Configuration required for authenticating via AWS Role Based Authentication
     */
    roleBasedAuth?: pulumi.Input<inputs.AwsConnectionRoleBasedAuth>;
    /**
     * Configuration required for authenticating via AWS Web Identity
     */
    webIdentity?: pulumi.Input<inputs.AwsConnectionWebIdentity>;
}

/**
 * The set of arguments for constructing a AwsConnection resource.
 */
export interface AwsConnectionArgs {
    /**
     * Name
     */
    name?: pulumi.Input<string>;
    /**
     * Configuration required for authenticating via AWS Role Based Authentication
     */
    roleBasedAuth?: pulumi.Input<inputs.AwsConnectionRoleBasedAuth>;
    /**
     * Configuration required for authenticating via AWS Web Identity
     */
    webIdentity?: pulumi.Input<inputs.AwsConnectionWebIdentity>;
}
