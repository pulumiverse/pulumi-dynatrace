// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * The Dynatrace GCP Principal resource provisions the Dynatrace GCP Principal (a service account) and exposes it via the `principal` attribute.
 * The principal must be granted the `roles/iam.serviceAccountTokenCreator` role on the GCP service account you want to impersonate, so that the `dynatrace.GcpConnection` resource can authenticate via service account impersonation.
 *
 * > This resource takes **no input**. The Dynatrace GCP Principal is a tenant-wide singleton that is provisioned by Dynatrace; this resource triggers its creation (if it does not exist yet), waits until it becomes available, and returns it. Creating the resource when a principal already exists is a no-op that simply reads the existing one.
 *
 * > `update` and `delete` are no-ops. Because the principal is a Dynatrace-managed singleton, `terraform destroy` removes the resource from state but leaves the principal in place.
 *
 * > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`), and the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`).
 *
 * > **Where the write permission applies is counter-intuitive.** Provisioning is triggered by a call against the **GCP connection** schema (`builtin:hyperscaler-authentication.connections.gcp`), so the **Write settings** permission must be granted on **that** schema. Write access on the GCP principal schema (`builtin:hyperscaler-authentication.connections.gcp-dynatrace-principal`) is **not required at all** — a caller who is denied write access on the principal schema can still provision the principal through this resource. This is a Dynatrace API characteristic and cannot be changed by the provider.
 *
 * ## Dynatrace Documentation
 *
 * - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:hyperscaler-authentication.connections.gcp-dynatrace-principal`)
 *
 * ## Resource Example Usage
 *
 * This example provisions the Dynatrace GCP Principal, grants it access to the impersonated service account, and wires it into a `dynatrace.GcpConnection`.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 * import * as gcp from "@pulumi/gcp";
 *
 * const config = new pulumi.Config();
 * // The Google Cloud Platform project ID
 * const gcpProjectId = config.require("gcpProjectId");
 * // Create a Service Account in GCP
 * const impersonableServiceAccount = new gcp.serviceaccount.Account("impersonable_service_account", {
 *     accountId: "test-service-account",
 *     displayName: "Test Service Account",
 * });
 * // Provision the DT GCP Principal. This resource takes no input; it triggers creation of the
 * // Dynatrace-managed principal (a singleton) and exposes it via the `principal` attribute.
 * const principal = new dynatrace.GcpPrincipal("principal", {});
 * // Grant DT GCP Principal access to the service account
 * const wifBinding = new gcp.serviceaccount.IAMMember("wif_binding", {
 *     serviceAccountId: impersonableServiceAccount.name,
 *     member: pulumi.interpolate`serviceAccount:${principal.principal}`,
 *     role: "roles/iam.serviceAccountTokenCreator",
 * });
 * // Create GCP connection
 * const myGcpConnection = new dynatrace.GcpConnection("my_gcp_connection", {
 *     name: "My Gcp Connection",
 *     type: "serviceAccountImpersonation",
 *     serviceAccountImpersonation: {
 *         serviceAccountId: impersonableServiceAccount.email,
 *         consumers: ["SVC:com.dynatrace.da"],
 *     },
 * }, {
 *     dependsOn: [wifBinding],
 * });
 * ```
 */
export class GcpPrincipal extends pulumi.CustomResource {
    /**
     * Get an existing GcpPrincipal resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: GcpPrincipalState, opts?: pulumi.CustomResourceOptions): GcpPrincipal {
        return new GcpPrincipal(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'dynatrace:index/gcpPrincipal:GcpPrincipal';

    /**
     * Returns true if the given object is an instance of GcpPrincipal.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is GcpPrincipal {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === GcpPrincipal.__pulumiType;
    }

    /**
     * Dynatrace GCP Service Account
     */
    declare public /*out*/ readonly principal: pulumi.Output<string>;

    /**
     * Create a GcpPrincipal resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: GcpPrincipalArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: GcpPrincipalArgs | GcpPrincipalState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as GcpPrincipalState | undefined;
            resourceInputs["principal"] = state?.principal;
        } else {
            const args = argsOrState as GcpPrincipalArgs | undefined;
            resourceInputs["principal"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(GcpPrincipal.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering GcpPrincipal resources.
 */
export interface GcpPrincipalState {
    /**
     * Dynatrace GCP Service Account
     */
    principal?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a GcpPrincipal resource.
 */
export interface GcpPrincipalArgs {
}
