// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`)
 *
 * > This resource requires the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`)
 *
 * ## Requirements
 *
 * This resource uses service account impersonation, which requires the Dynatrace GCP Principal to be granted the `roles/iam.serviceAccountTokenCreator` role on the impersonated service account.
 * Use the `dynatrace.GcpPrincipal` resource to provision the Dynatrace GCP Principal and grant it access before creating the connection.
 * A full example of how to set this up can be found in the Resource Example Usage section below.
 *
 * ## Limitations
 *
 * If you are creating the `dynatrace.GcpConnection` and the IAM binding that grants the Dynatrace GCP Principal access to the service account in the same invocation
 * of `pulumi up`, be aware that due to eventual consistency in GCP, the IAM policy change might not be fully propagated
 * when the `dynatrace.GcpConnection` resource is being created. To mitigate this, we retry the creation of the `dynatrace.GcpConnection` resource
 * with a default timeout of 2 minutes.
 * If you desire a different timeout, you can adjust it using the `timeouts` block in the `dynatrace.GcpConnection` resource.
 *
 * The following is an example of the error you might encounter due to this limitation:
 *
 * > **Warning** If a resource is created using an API token or without setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true` (when both are used), the settings object's owner will remain empty.
 *
 * An empty owner implies:
 * - The settings object becomes public, allowing other users with settings permissions to read and modify it.
 * - Changing the settings object's permissions will have no effect, meaning the `dynatrace.SettingsPermissions` resource can't alter its access.
 *
 * When a settings object is created using platform credentials:
 * - The owner is set to the owner of the OAuth client or platform token.
 * - By default, the settings object is private; only the owner can read and modify it.
 * - Access modifiers can be managed using the `dynatrace.SettingsPermissions` resource.
 *
 * We recommend using platform credentials to ensure a correct setup.
 * In case an API token is needed, we recommend setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true`.
 *
 * ## Dynatrace Documentation
 *
 * - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:hyperscaler-authentication.connections.gcp`)
 *
 * ## Export Example Usage
 *
 * - `terraform-provider-dynatrace -export dynatrace.GcpConnection` downloads all existing GCP connections.
 *
 * The full documentation of the export feature is available [here](https://dt-url.net/h203qmc).
 *
 * ## Resource Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 * import * as gcp from "@pulumi/gcp";
 *
 * const config = new pulumi.Config();
 * // The Google Cloud Platform project ID
 * const gcpProjectId = config.require("gcpProjectId");
 * // Create a Service Account in GCP
 * const impersonableServiceAccount = new gcp.serviceaccount.Account("impersonable_service_account", {
 *     accountId: "test-service-account",
 *     displayName: "Test Service Account",
 * });
 * // Provision the DT GCP Principal. This resource takes no input; it triggers creation of the
 * // Dynatrace-managed principal (a singleton) and exposes it via the `principal` attribute.
 * const principal = new dynatrace.GcpPrincipal("principal", {});
 * // Grant DT GCP Principal access to the service account
 * const wifBinding = new gcp.serviceaccount.IAMMember("wif_binding", {
 *     serviceAccountId: impersonableServiceAccount.name,
 *     member: pulumi.interpolate`serviceAccount:${principal.principal}`,
 *     role: "roles/iam.serviceAccountTokenCreator",
 * });
 * // Create GCP connection
 * const myGcpConnection = new dynatrace.GcpConnection("my_gcp_connection", {
 *     name: "My Gcp Connection",
 *     type: "serviceAccountImpersonation",
 *     serviceAccountImpersonation: {
 *         serviceAccountId: impersonableServiceAccount.email,
 *         consumers: ["SVC:com.dynatrace.da"],
 *     },
 * }, {
 *     dependsOn: [wifBinding],
 * });
 * ```
 */
export class GcpConnection extends pulumi.CustomResource {
    /**
     * Get an existing GcpConnection resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: GcpConnectionState, opts?: pulumi.CustomResourceOptions): GcpConnection {
        return new GcpConnection(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'dynatrace:index/gcpConnection:GcpConnection';

    /**
     * Returns true if the given object is an instance of GcpConnection.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is GcpConnection {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === GcpConnection.__pulumiType;
    }

    /**
     * The name of the connection
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * No documentation available
     */
    declare public readonly serviceAccountImpersonation: pulumi.Output<outputs.GcpConnectionServiceAccountImpersonation | undefined>;
    /**
     * GCP Authentication mechanism to be used by the connection. Possible values: `serviceAccountImpersonation`
     */
    declare public readonly type: pulumi.Output<string>;

    /**
     * Create a GcpConnection resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: GcpConnectionArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: GcpConnectionArgs | GcpConnectionState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as GcpConnectionState | undefined;
            resourceInputs["name"] = state?.name;
            resourceInputs["serviceAccountImpersonation"] = state?.serviceAccountImpersonation;
            resourceInputs["type"] = state?.type;
        } else {
            const args = argsOrState as GcpConnectionArgs | undefined;
            if (args?.type === undefined && !opts.urn) {
                throw new Error("Missing required property 'type'");
            }
            resourceInputs["name"] = args?.name;
            resourceInputs["serviceAccountImpersonation"] = args?.serviceAccountImpersonation;
            resourceInputs["type"] = args?.type;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(GcpConnection.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering GcpConnection resources.
 */
export interface GcpConnectionState {
    /**
     * The name of the connection
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * No documentation available
     */
    serviceAccountImpersonation?: pulumi.Input<inputs.GcpConnectionServiceAccountImpersonation | undefined>;
    /**
     * GCP Authentication mechanism to be used by the connection. Possible values: `serviceAccountImpersonation`
     */
    type?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a GcpConnection resource.
 */
export interface GcpConnectionArgs {
    /**
     * The name of the connection
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * No documentation available
     */
    serviceAccountImpersonation?: pulumi.Input<inputs.GcpConnectionServiceAccountImpersonation | undefined>;
    /**
     * GCP Authentication mechanism to be used by the connection. Possible values: `serviceAccountImpersonation`
     */
    type: pulumi.Input<string>;
}
