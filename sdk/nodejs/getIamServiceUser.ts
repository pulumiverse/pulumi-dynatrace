// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * > **Dynatrace SaaS only**
 *
 * > To utilize this resource, please define the environment variables `DT_CLIENT_ID`, `DT_CLIENT_SECRET`, `DT_ACCOUNT_ID` with an OAuth client including the following permission: **Allow read access for identity resources (users and groups)** (`account-idm-read`).
 *
 * This data source allows you to query a service user by ID, name, or email address. Note: as the name of a service user is not necessarily unique, the data source will produce an error if two or more service users exist with the specified name.
 *
 * ## Example Usage
 *
 * Query a service user by name:
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * export = async () => {
 *     const testServiceUser = await dynatrace.getIamServiceUser({
 *         name: "test",
 *     });
 *     return {
 *         testServiceUser: testServiceUser,
 *     };
 * }
 * ```
 *
 * Query a service user by ID:
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * export = async () => {
 *     const testServiceUser = await dynatrace.getIamServiceUser({
 *         id: "12345678-90ab-cdef-1234-567890abcdef",
 *     });
 *     return {
 *         testServiceUser: testServiceUser,
 *     };
 * }
 * ```
 *
 * Query a service user by email:
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * export = async () => {
 *     const testServiceUser = await dynatrace.getIamServiceUser({
 *         email: "12345678-90ab-cdef-1234-567890abcdef@service.sso.dynatrace.com",
 *     });
 *     return {
 *         testServiceUser: testServiceUser,
 *     };
 * }
 * ```
 *
 * ## Example Output
 */
export function getIamServiceUser(args?: GetIamServiceUserArgs, opts?: pulumi.InvokeOptions): Promise<GetIamServiceUserResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("dynatrace:index/getIamServiceUser:getIamServiceUser", {
        "email": args.email,
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getIamServiceUser.
 */
export interface GetIamServiceUserArgs {
    /**
     * The email of the service user
     */
    email?: string;
    /**
     * The UUID of the service user
     */
    id?: string;
    /**
     * The name of the service user
     */
    name?: string;
}

/**
 * A collection of values returned by getIamServiceUser.
 */
export interface GetIamServiceUserResult {
    /**
     * The description of the service user
     */
    readonly description: string;
    /**
     * The email of the service user
     */
    readonly email?: string;
    /**
     * The UUIDs of the groups the service user belongs to
     */
    readonly groups: string[];
    /**
     * The UUID of the service user
     */
    readonly id?: string;
    /**
     * The name of the service user
     */
    readonly name?: string;
}
/**
 * > **Dynatrace SaaS only**
 *
 * > To utilize this resource, please define the environment variables `DT_CLIENT_ID`, `DT_CLIENT_SECRET`, `DT_ACCOUNT_ID` with an OAuth client including the following permission: **Allow read access for identity resources (users and groups)** (`account-idm-read`).
 *
 * This data source allows you to query a service user by ID, name, or email address. Note: as the name of a service user is not necessarily unique, the data source will produce an error if two or more service users exist with the specified name.
 *
 * ## Example Usage
 *
 * Query a service user by name:
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * export = async () => {
 *     const testServiceUser = await dynatrace.getIamServiceUser({
 *         name: "test",
 *     });
 *     return {
 *         testServiceUser: testServiceUser,
 *     };
 * }
 * ```
 *
 * Query a service user by ID:
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * export = async () => {
 *     const testServiceUser = await dynatrace.getIamServiceUser({
 *         id: "12345678-90ab-cdef-1234-567890abcdef",
 *     });
 *     return {
 *         testServiceUser: testServiceUser,
 *     };
 * }
 * ```
 *
 * Query a service user by email:
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * export = async () => {
 *     const testServiceUser = await dynatrace.getIamServiceUser({
 *         email: "12345678-90ab-cdef-1234-567890abcdef@service.sso.dynatrace.com",
 *     });
 *     return {
 *         testServiceUser: testServiceUser,
 *     };
 * }
 * ```
 *
 * ## Example Output
 */
export function getIamServiceUserOutput(args?: GetIamServiceUserOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetIamServiceUserResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("dynatrace:index/getIamServiceUser:getIamServiceUser", {
        "email": args.email,
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getIamServiceUser.
 */
export interface GetIamServiceUserOutputArgs {
    /**
     * The email of the service user
     */
    email?: pulumi.Input<string>;
    /**
     * The UUID of the service user
     */
    id?: pulumi.Input<string>;
    /**
     * The name of the service user
     */
    name?: pulumi.Input<string>;
}
