// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`)
 *
 * > This resource requires the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`)
 *
 * ## Requirements
 *
 * This resource must be used in combination with the `dynatrace.AzureConnection` resource to manage a connection using federated identity credential.
 * An example of how to set up both resources can be found in the Resource Example Usage section below.
 *
 * ## Limitations
 *
 * If you are creating the `dynatrace.AzureConnectionAuthentication` and the `azureadApplicationFederatedIdentityCredential` it is referencing in the same invocation of `pulumi up`, be aware that due to eventual consistency in Azure, the creation of the federated identity credential might not be fully propagated when the `dynatrace.AzureConnectionAuthentication` resource is being created. To mitigate this, we retry the creation of the `dynatrace.AzureConnectionAuthentication` resource with a default timeout of 2 minutes.
 * If you desire a different timeout, you can adjust it using the `timeouts` block in the `dynatrace.AzureConnectionAuthentication` resource as shown in the example below.
 *
 * The following is an example of the error you might encounter due to this limitation:
 *
 * > **Warning** If a resource is created using an API token or without setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true` (when both are used), the settings object's owner will remain empty.
 *
 * An empty owner implies:
 * - The settings object becomes public, allowing other users with settings permissions to read and modify it.
 * - Changing the settings object's permissions will have no effect, meaning the `dynatrace.SettingsPermissions` resource can't alter its access.
 *
 * When a settings object is created using platform credentials:
 * - The owner is set to the owner of the OAuth client or platform token.
 * - By default, the settings object is private; only the owner can read and modify it.
 * - Access modifiers can be managed using the `dynatrace.SettingsPermissions` resource.
 *
 * We recommend using platform credentials to ensure a correct setup.
 * In case an API token is needed, we recommend setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true`.
 *
 * ## Dynatrace Documentation
 *
 * - [Set up Microsoft Azure Connector](https://dt-url.net/gs034de)
 *
 * - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:hyperscaler-authentication.connections.azure`)
 *
 * ## Export Example Usage
 *
 * - `terraform-provider-dynatrace -export dynatrace.AzureConnectionAuthentication` downloads all existing Azure connections.
 *
 * The full documentation of the export feature is available [here](https://dt-url.net/h203qmc).
 *
 * ## Resource Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuread from "@pulumi/azuread";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * const config = new pulumi.Config();
 * // The Azure Active Directory tenant ID.
 * const azureTenantId = config.require("azureTenantId");
 * // The Dynatrace environment URL
 * const dynatraceEnvironmentUrl = config.require("dynatraceEnvironmentUrl");
 * // The Dynatrace token issuer URL
 * const dynatraceTokenIssuer = config.require("dynatraceTokenIssuer");
 * // Create an application
 * const example = new azuread.ApplicationRegistration("example", {displayName: "ExampleApp"});
 * // Create basic Azure connection
 * const exampleAzureConnection = new dynatrace.AzureConnection("example", {
 *     name: "#name#",
 *     type: "federatedIdentityCredential",
 *     federatedIdentityCredential: {
 *         consumers: ["APP:dynatrace.microsoft.azure.connector"],
 *     },
 * });
 * // Create a federated identity credential
 * const exampleApplicationFederatedIdentityCredential = new azuread.ApplicationFederatedIdentityCredential("example", {
 *     applicationId: example.id,
 *     displayName: "Example",
 *     audiences: [`${dynatraceEnvironmentUrl}/app-id/dynatrace.microsoft.azure.connector`],
 *     issuer: dynatraceTokenIssuer,
 *     subject: pulumi.interpolate`dt:connection-id/${exampleAzureConnection.id}`,
 * });
 * // Update the Azure connection with authentication details
 * const exampleAzureConnectionAuthentication = new dynatrace.AzureConnectionAuthentication("example", {
 *     azureConnectionId: exampleAzureConnection.id,
 *     applicationId: example.clientId,
 *     directoryId: azureTenantId,
 * });
 * ```
 */
export class AzureConnectionAuthentication extends pulumi.CustomResource {
    /**
     * Get an existing AzureConnectionAuthentication resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AzureConnectionAuthenticationState, opts?: pulumi.CustomResourceOptions): AzureConnectionAuthentication {
        return new AzureConnectionAuthentication(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'dynatrace:index/azureConnectionAuthentication:AzureConnectionAuthentication';

    /**
     * Returns true if the given object is an instance of AzureConnectionAuthentication.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AzureConnectionAuthentication {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AzureConnectionAuthentication.__pulumiType;
    }

    /**
     * Application (client) ID of your app registered in Microsoft Azure App registrations
     */
    declare public readonly applicationId: pulumi.Output<string>;
    /**
     * The ID of a `dynatrace.AzureConnection` resource instance for which to define the Azure Authentication
     */
    declare public readonly azureConnectionId: pulumi.Output<string>;
    /**
     * Directory (tenant) ID of Microsoft Entra ID
     */
    declare public readonly directoryId: pulumi.Output<string>;

    /**
     * Create a AzureConnectionAuthentication resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AzureConnectionAuthenticationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AzureConnectionAuthenticationArgs | AzureConnectionAuthenticationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AzureConnectionAuthenticationState | undefined;
            resourceInputs["applicationId"] = state?.applicationId;
            resourceInputs["azureConnectionId"] = state?.azureConnectionId;
            resourceInputs["directoryId"] = state?.directoryId;
        } else {
            const args = argsOrState as AzureConnectionAuthenticationArgs | undefined;
            if (args?.applicationId === undefined && !opts.urn) {
                throw new Error("Missing required property 'applicationId'");
            }
            if (args?.azureConnectionId === undefined && !opts.urn) {
                throw new Error("Missing required property 'azureConnectionId'");
            }
            if (args?.directoryId === undefined && !opts.urn) {
                throw new Error("Missing required property 'directoryId'");
            }
            resourceInputs["applicationId"] = args?.applicationId;
            resourceInputs["azureConnectionId"] = args?.azureConnectionId;
            resourceInputs["directoryId"] = args?.directoryId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AzureConnectionAuthentication.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AzureConnectionAuthentication resources.
 */
export interface AzureConnectionAuthenticationState {
    /**
     * Application (client) ID of your app registered in Microsoft Azure App registrations
     */
    applicationId?: pulumi.Input<string>;
    /**
     * The ID of a `dynatrace.AzureConnection` resource instance for which to define the Azure Authentication
     */
    azureConnectionId?: pulumi.Input<string>;
    /**
     * Directory (tenant) ID of Microsoft Entra ID
     */
    directoryId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a AzureConnectionAuthentication resource.
 */
export interface AzureConnectionAuthenticationArgs {
    /**
     * Application (client) ID of your app registered in Microsoft Azure App registrations
     */
    applicationId: pulumi.Input<string>;
    /**
     * The ID of a `dynatrace.AzureConnection` resource instance for which to define the Azure Authentication
     */
    azureConnectionId: pulumi.Input<string>;
    /**
     * Directory (tenant) ID of Microsoft Entra ID
     */
    directoryId: pulumi.Input<string>;
}
