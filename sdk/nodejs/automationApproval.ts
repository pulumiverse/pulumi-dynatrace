// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`)
 *
 * ## Dynatrace Documentation
 *
 * - Dynatrace Workflows - https://www.dynatrace.com/support/help/platform-modules/cloud-automation/workflows
 *
 * - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:automation.approval)
 */
export class AutomationApproval extends pulumi.CustomResource {
    /**
     * Get an existing AutomationApproval resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AutomationApprovalState, opts?: pulumi.CustomResourceOptions): AutomationApproval {
        return new AutomationApproval(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'dynatrace:index/automationApproval:AutomationApproval';

    /**
     * Returns true if the given object is an instance of AutomationApproval.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AutomationApproval {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AutomationApproval.__pulumiType;
    }

    /**
     * Allow on tenant level anyone with access to the app can respond to requests via an approval link.
     */
    declare public readonly workflowAppAccessApprovalEnabled: pulumi.Output<boolean>;

    /**
     * Create a AutomationApproval resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AutomationApprovalArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AutomationApprovalArgs | AutomationApprovalState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AutomationApprovalState | undefined;
            resourceInputs["workflowAppAccessApprovalEnabled"] = state?.workflowAppAccessApprovalEnabled;
        } else {
            const args = argsOrState as AutomationApprovalArgs | undefined;
            if (args?.workflowAppAccessApprovalEnabled === undefined && !opts.urn) {
                throw new Error("Missing required property 'workflowAppAccessApprovalEnabled'");
            }
            resourceInputs["workflowAppAccessApprovalEnabled"] = args?.workflowAppAccessApprovalEnabled;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AutomationApproval.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AutomationApproval resources.
 */
export interface AutomationApprovalState {
    /**
     * Allow on tenant level anyone with access to the app can respond to requests via an approval link.
     */
    workflowAppAccessApprovalEnabled?: pulumi.Input<boolean>;
}

/**
 * The set of arguments for constructing a AutomationApproval resource.
 */
export interface AutomationApprovalArgs {
    /**
     * Allow on tenant level anyone with access to the app can respond to requests via an approval link.
     */
    workflowAppAccessApprovalEnabled: pulumi.Input<boolean>;
}
