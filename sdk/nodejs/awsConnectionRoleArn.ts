// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * > This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`)
 *
 * > This resource requires the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`)
 *
 * ## Requirements
 *
 * This resource must be used in combination with the `dynatrace.AwsConnection` resource.
 * Ensure you configure both resources together for a valid AWS connection.
 * An example of how to set up both resources can be found in the Resource Example Usage section below.
 *
 * ## Limitations
 *
 * If you are creating the `dynatrace.AwsConnectionRoleArn` and the `awsIamRole` it is referencing in the same invocation
 * of `pulumi up`, be aware that due to eventual consistency in AWS, the creation of the `awsIamRole` might not be fully propagated
 * when the `dynatrace.AwsConnectionRoleArn` resource is being created. To mitigate this, we retry the creation of the `dynatrace.AwsConnectionRoleArn` resource
 * with a default timeout of 2 minutes.
 * This is also the timeout used by terraform-provider-aws when waiting for IAM changes to propagate.
 * If you desire a different timeout, you can adjust it using the `timeouts` block in the `dynatrace.AwsConnectionRoleArn` resource as shown in the example below.
 *
 * The following is an example of the error you might encounter due to this limitation:
 *
 * > **Warning** If a resource is created using an API token or without setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true` (when both are used), the settings object's owner will remain empty.
 *
 * An empty owner implies:
 * - The settings object becomes public, allowing other users with settings permissions to read and modify it.
 * - Changing the settings object's permissions will have no effect, meaning the `dynatrace.SettingsPermissions` resource can't alter its access.
 *
 * When a settings object is created using platform credentials:
 * - The owner is set to the owner of the OAuth client or platform token.
 * - By default, the settings object is private; only the owner can read and modify it.
 * - Access modifiers can be managed using the `dynatrace.SettingsPermissions` resource.
 *
 * We recommend using platform credentials to ensure a correct setup.
 * In case an API token is needed, we recommend setting `DYNATRACE_HTTP_OAUTH_PREFERENCE=true`.
 *
 * ## Dynatrace Documentation
 *
 * - AWS Connector - https://docs.dynatrace.com/docs/analyze-explore-automate/workflows/actions/aws
 *
 * - Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:hyperscaler-authentication.connections.aws`)
 *
 * ## Export Example Usage
 *
 * - `terraform-provider-dynatrace -export dynatrace.AwsConnectionRoleArn` downloads all existing AWS connections.
 *
 * The full documentation of the export feature is available [here](https://dt-url.net/h203qmc).
 *
 * ## Resource Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 * import * as dynatrace from "@pulumiverse/dynatrace";
 *
 * const test_aws_connection = new dynatrace.AwsConnection("test-aws-connection", {
 *     name: "#name#",
 *     webIdentity: {
 *         consumers: "APP:dynatrace.aws.connector",
 *     },
 * });
 * const dynatrace_oidc_provider = new aws.iam.OpenIdConnectProvider("dynatrace-oidc-provider", {
 *     url: "https://token.dynatrace.com",
 *     clientIdLists: ["<TENANT_URL>/app-id/dynatrace.aws.connector"],
 * });
 * const exampleRole = new aws.iam.Role("example_role", {
 *     name: "#name#",
 *     assumeRolePolicy: pulumi.jsonStringify({
 *         Version: "2012-10-17",
 *         Statement: [{
 *             Effect: "Allow",
 *             Principal: {
 *                 Federated: dynatrace_oidc_provider.arn,
 *             },
 *             Action: "sts:AssumeRoleWithWebIdentity",
 *             Condition: {
 *                 StringEquals: pulumi.all([dynatrace_oidc_provider.url, test_aws_connection.id, dynatrace_oidc_provider.url]).apply(([dynatrace-oidc-providerUrl, id, dynatrace-oidc-providerUrl1]) => {
 *                     [`${dynatrace_oidc_providerUrl}:sub`]: `dt:connection-id/${id}`,
 *                     [`${dynatrace_oidc_providerUrl1}:aud`]: "<TENANT_URL>/app-id/dynatrace.aws.connector",
 *                 }),
 *             },
 *         }],
 *     }),
 * });
 * const test_aws_connection_arn = new dynatrace.AwsConnectionRoleArn("test-aws-connection-arn", {
 *     awsConnectionId: test_aws_connection.id,
 *     roleArn: exampleRole.arn,
 * });
 * ```
 */
export class AwsConnectionRoleArn extends pulumi.CustomResource {
    /**
     * Get an existing AwsConnectionRoleArn resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AwsConnectionRoleArnState, opts?: pulumi.CustomResourceOptions): AwsConnectionRoleArn {
        return new AwsConnectionRoleArn(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'dynatrace:index/awsConnectionRoleArn:AwsConnectionRoleArn';

    /**
     * Returns true if the given object is an instance of AwsConnectionRoleArn.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AwsConnectionRoleArn {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AwsConnectionRoleArn.__pulumiType;
    }

    /**
     * The ID of a `dynatrace.AwsConnection` resource instance for which to define the AWS Role ARN
     */
    declare public readonly awsConnectionId: pulumi.Output<string>;
    /**
     * The ARN of the AWS role that should be assumed.
     */
    declare public readonly roleArn: pulumi.Output<string>;

    /**
     * Create a AwsConnectionRoleArn resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AwsConnectionRoleArnArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AwsConnectionRoleArnArgs | AwsConnectionRoleArnState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AwsConnectionRoleArnState | undefined;
            resourceInputs["awsConnectionId"] = state?.awsConnectionId;
            resourceInputs["roleArn"] = state?.roleArn;
        } else {
            const args = argsOrState as AwsConnectionRoleArnArgs | undefined;
            if (args?.awsConnectionId === undefined && !opts.urn) {
                throw new Error("Missing required property 'awsConnectionId'");
            }
            if (args?.roleArn === undefined && !opts.urn) {
                throw new Error("Missing required property 'roleArn'");
            }
            resourceInputs["awsConnectionId"] = args?.awsConnectionId;
            resourceInputs["roleArn"] = args?.roleArn;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AwsConnectionRoleArn.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AwsConnectionRoleArn resources.
 */
export interface AwsConnectionRoleArnState {
    /**
     * The ID of a `dynatrace.AwsConnection` resource instance for which to define the AWS Role ARN
     */
    awsConnectionId?: pulumi.Input<string>;
    /**
     * The ARN of the AWS role that should be assumed.
     */
    roleArn?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a AwsConnectionRoleArn resource.
 */
export interface AwsConnectionRoleArnArgs {
    /**
     * The ID of a `dynatrace.AwsConnection` resource instance for which to define the AWS Role ARN
     */
    awsConnectionId: pulumi.Input<string>;
    /**
     * The ARN of the AWS role that should be assumed.
     */
    roleArn: pulumi.Input<string>;
}
